#include <bits/stdc++.h>
using namespace std;
void stuff(vector<string> s){
    // Stuck on how to progress
}
int main(){
    vector<string> s;
    for(int i=0;i<s.size();i++){
        cin >> s[i];
    }
    stuff(s);
}